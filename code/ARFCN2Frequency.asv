% Converts ARFCN to Hz
function centerFrequency = ARFCN2Frequency(ARFCN)

fRefOffset = 0;
nRefOffset = 0;
deltaFGlobal = 0;

if ARFCN >= 0 && ARFCN < 600000
    deltaFGlobal = 5000;

    centerFrequency = fRefOffset + deltaFGlobal * (ARFCN - nRefOffset);

elseif ARFCN >= 600000 && ARFCN < 2016667
    fRefOffset = 3e9
    nRefOffset = 0;
    deltaFGlobal = 15000;


end

%FRef = FRefOffset + DeltaFGlobal * (NRARFCN - NRefOffset);

end
